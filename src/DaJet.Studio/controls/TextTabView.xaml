<UserControl x:Class="DaJet.Studio.UI.TextTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="TextTabViewTextBox" Grid.Row="0"
                 Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=Explicit}"
                 FontFamily="Courier New" FontSize="14" Margin="0,5,0,0"
                 TextWrapping="Wrap" AcceptsTab="True" AcceptsReturn="True">
        </TextBox>
        
    </Grid>
    
</UserControl>
